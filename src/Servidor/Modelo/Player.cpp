#include <iostream>
#include "Player.h"

/*Player::Player(Map& map,Game* game) : character(100, 1, 1, map,game) {}
bool Player::exists(std::string name) {
    if (this->name == name) return true;
    return false;
}

void Player::move(Offset offset) {
    character.move(offset);
}

Player::~Player() = default;*/
